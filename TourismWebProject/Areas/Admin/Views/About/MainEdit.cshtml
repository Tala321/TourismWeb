
@{
    ViewBag.Title = "MainEdit";
}
<div id="wrapper">
    @Html.Partial("SideBarPartial")
    <div id="content-wrapper">
        <table " id="dataTable"></table>
        <div class="container-fluid">
            <div class="card mb-3">
                <div class="card-header">
                    <i class="fas fa-table"></i>
                    <strong> About page main part</strong>
                </div>
                <div class="card-body">
                    <div class="table-responsive ">
                        <table class="table table-striped  " id="dataTable" width="100%" cellspacing="0">
                            @using (Html.BeginForm("MainEdit", "About", FormMethod.Post, new { enctype = "multipart/form-data" }))
                            {
                                <div class="form-group ">
                                    <label for="AboutPageBackPic">Main Photo of the background</label>
                                    <input type="file" class="form-control" name="AboutPageBackPic">
                                </div>
                                <div class="form-group ">
                                    <label for="AboutPagePic">Photo of the content</label>
                                    <input type="file" class="form-control" name="AboutPagePic">
                                </div>
                                <div class="form-group">
                                    <label for="AboutPageBackPicText">Text for the background photo</label>
                                    <textarea type="textarea"  required="" class="form-control" name="AboutPageBackPicText">@ViewData["AboutPageBackPicText"]</textarea>
                                </div>
                                <button type="submit" class="btn btn-primary">Submit</button>
                            }
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>



